# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

# editor
.vscode/*
!.vscode/extensions.json
.idea

# the original brand/source assets are reference material, not app code.
# this folder holds CONFIDENTIAL material (real client NDAs, business plan,
# financial model, investor deck, one-pager, advisory board deck, admin email
# list, client pitch decks/briefings). it must never be committed or pushed.
# web-ready copies the app actually uses live in /public.
/assets/
