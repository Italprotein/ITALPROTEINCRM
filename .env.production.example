# Copy to .env.production on the VPS and fill in. NEVER commit the real file.
#   docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build

# Domain Caddy serves and the app's own origin.
APP_DOMAIN=crm.italprotein.com
APP_URL=https://crm.italprotein.com

# Database used by both the Postgres container and the app.
# Strong, random. The app's DATABASE_URL is assembled from this in compose.
POSTGRES_PASSWORD=REPLACE_with_a_long_random_password

# Auth and crypto secrets. Generate fresh values; never reuse examples.
# npx auth secret -> AUTH_SECRET
# node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))" -> the keys below
AUTH_SECRET=REPLACE_with_a_long_random_secret
AUTH_COOKIE_DOMAIN=crm.italprotein.com
GOOGLE_TOKEN_ENC_KEY=REPLACE_with_a_long_random_secret
CRON_SECRET=REPLACE_with_a_long_random_secret

# Public site/contact values baked into the client bundle at build.
NEXT_PUBLIC_SITE_URL=https://www.italprotein.com
NEXT_PUBLIC_CONTACT_EMAIL=ad@italprotein.com
NEXT_PUBLIC_CONTACT_PHONE_PRIMARY=+39 351 910 3211
NEXT_PUBLIC_CONTACT_ADDRESS=Galleria Cavour 7, Bologna, Italy

# Google Workspace OAuth for Gmail send/sync. Leave blank until configured.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://crm.italprotein.com/api/auth/google/callback
GMAIL_SENDER_EMAIL=ad@italprotein.com
GMAIL_REPLY_TO=ad@italprotein.com

# Object storage for uploaded documents (S3-compatible).
# Required before external company portal users may upload/download files.
# Works with Cloudflare R2, Backblaze B2, AWS S3, or a self-hosted MinIO.
# The bucket MUST be private. The app streams every file through
# /api/attachments/[id] after an authorization check and never mints public URLs.
#   R2:  ENDPOINT=https://<account-id>.r2.cloudflarestorage.com   REGION=auto
#   B2:  ENDPOINT=https://s3.<region>.backblazeb2.com             REGION=<region>
#   S3:  leave ENDPOINT blank                                     REGION=eu-central-1
# Leave all blank to keep file bytes inside Postgres instead.
OBJECT_STORAGE_BUCKET=
OBJECT_STORAGE_REGION=auto
OBJECT_STORAGE_ENDPOINT=
OBJECT_STORAGE_ACCESS_KEY_ID=
OBJECT_STORAGE_SECRET_ACCESS_KEY=

# AI provider: use "groq" for its free tier, or "openai" for OpenAI billing.
AI_PROVIDER=groq
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-20b

# Reserved legacy provider settings (not used by Amina or AI tasks).
AI_MODEL=claude-opus-4-8
ANTHROPIC_API_KEY=

# Optional paid OpenAI fallback.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-sol
